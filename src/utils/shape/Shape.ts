import { TPoint } from "~utils/math/Point/Point.types"
import { TShape } from "./Shape.types"

export const computeShapePoints = (shape: TShape): TPoint[] => {
  // 1. Get base points
  // 2. Apply base noise
  // 3. Apply stylization

  return []
}
